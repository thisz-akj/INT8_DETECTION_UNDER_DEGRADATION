# Tested with Python 3.12. Install with: pip install -r requirements.txt
# (torch/torchvision come in automatically as ultralytics dependencies; a CPU-only
# torch wheel is sufficient and lighter -- everything in this repo runs inference
# through ONNX Runtime's CPUExecutionProvider, never through torch/GPU.)
ultralytics==8.4.146
onnx==1.22.0
onnxruntime==1.29.0
onnxslim==0.1.96
opencv-python-headless==5.0.0.93
numpy==2.5.3
pycocotools==2.0.11
matplotlib==3.11.1
python-docx==1.2.0
